<div class="grid grid-cols-8 grid-rows-8 gap-4">
    <div class="col-span-8">
      <h1 class="mb-4 text-4xl font-extrabold leading-none tracking-tight text-gray-900 md:text-5xl lg:text-6xl dark:text-white">Year {{year.name}}</span></h1>
    </div>
    <div class="col-span-8 row-span-7 row-start-2">
      {{#each items as |item|}}
      {{> item-details item}}
      {{/each}}
    </div>
</div>
